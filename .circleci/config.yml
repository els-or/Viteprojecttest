version: 2.1

jobs:
  start-server:
    docker:
    steps:
      -checkout
      - runs: 
          name: Install dependencies
          command: npm install
      - run:
          name: Build the app
          command: npm run build


workflows:
  run-tests:
    jobs:
      - start-server
      -cypress/run
         name: run-componet-tests
         cypress-command: npx cypress run --component
